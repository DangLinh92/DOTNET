@model TotalVOCSiteModel
<div class="row">
    <div class="col-sm-12">
        <ul class="breadcrumb">
            <li class="breadcrumb-item active"><i class="fa fa-caret-right"></i> <strong>@Model.Year.ToString().Substring(2)년 Total VOC 건수그래프 (제품 생산 Site 기준)</strong></li>
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-lg-12 col-xl-4 d-flex">
    </div>
    <!-- Chart total by year-->
    <div class="col-md-12 col-lg-12 col-xl-4 d-flex">
        <div class="card flex-fill dash-statistics">
            <div class="card-header">
                <h5 class="card-title mb-0">@Model.Year.ToString().Substring(2)년 VOC</h5>
            </div>
            <div class="card-body">
                <div id="s-col-stacked"></div>
            </div>
        </div>
    </div>
    <!-- /Chart -->
    <!-- data -->
    <div class="col-md-4 d-flex">
        <div class="card card-table flex-fill">
            <div class="card-header">
                <h3 class="card-title mb-0">@Model.Year.ToString().Substring(2)년 VOC</h3>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover mb-0 table-striped custom-table datatable">
                        <thead>
                            <tr>
                                <th>구분</th>
                                @foreach (var item in Model.Divisions)
                                {
                                    <th>@item</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                @foreach (var item in Model.PartsClassification)
                                {
                                    <tr>
                                        <td>@item</td>
                                        @foreach (TotalVOCSiteModelItem voc in Model.totalVOCSiteModelItems)
                                        {
                                            if (voc.Classification == item)
                                            {
                                                <td>@voc.Qty</td>
                                            }
                                        }
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="card-footer">
                <strong style="float:left">Total: @Model.Total</strong>
                <i style="float:right">
                    ※ 와이솔 제품 아닌 경우, 카운트하지 않음.
                </i>
            </div>
        </div>
    </div>
</div>
